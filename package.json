{
  "name": "burbot-firefox-extension",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "start": "npm run build && web-ext run --source-dir dist --target firefox-desktop",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node --test tests/extraction-contracts.test.mjs tests/geography-contracts.test.mjs tests/file-source-contracts.test.mjs tests/pdf-selector-contracts.test.mjs",
    "lint": "npm run build && npm run typecheck && web-ext lint --source-dir dist",
    "format": "prettier --write src scripts tests/extraction-contracts.test.mjs tests/geography-contracts.test.mjs tests/file-source-contracts.test.mjs tests/pdf-selector-contracts.test.mjs manifest.json package.json tsconfig.json README.md"
  },
  "dependencies": {
    "pdfjs-dist": "^6.3.289",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "esbuild": "^0.25.0",
    "prettier": "^3.6.0",
    "typescript": "^5.9.0",
    "web-ext": "^9.0.0"
  }
}
