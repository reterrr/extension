# Local SQLite file used by `npm run db`.
BURBOT_DB_PATH=./data/burbot.sqlite

# Keep the default host/port unless you also update the extension connection URL.
BURBOT_DB_HOST=127.0.0.1
BURBOT_DB_PORT=8765
